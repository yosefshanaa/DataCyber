[pytest]
# Make the project importable (`import src...`) without installing it.
pythonpath = .
testpaths = tests
markers =
    slow: integration tests that load the bundled NSL-KDD data (deselect with -m "not slow")
addopts = -ra
