# Python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# Raw data is fetchable via data/get_data.py. NSL-KDD (~6MB) is committed for
# one-clone reproducibility; UNSW-NB15 (~47MB) is also fetched by get_data.py.

# Report build intermediate (PDF is the deliverable)
report/report.html
